extends ../layouts/application.pug

block header
    div.text-center
        = t('views.label.new')

block content
    div.w-50.mx-auto
        +form('post')(action=route('labels/create'))
            +input(label, 'name', errors)(required)
            +submit()(value=t('buttons.new'))
